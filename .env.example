# ============================================
# QR ATTENDANCE SYSTEM - Environment Variables
# ============================================

# ===========================================
# SUPABASE (REQUIRED FOR PRODUCTION)
# ===========================================
# Obtén estas keys desde: Supabase Dashboard > Settings > API

# Frontend Variables (prefijo VITE_ para acceso desde el navegador)
VITE_SUPABASE_URL="https://[PROJECT-REF].supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-key"

# Backend Variables (para serverless functions)
SUPABASE_URL="https://[PROJECT-REF].supabase.co"
SUPABASE_ANON_KEY="your-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# ===========================================
# DATABASE (Supabase PostgreSQL)
# ===========================================
# Obtén estas URLs desde: Supabase Dashboard > Settings > Database

# Connection String (Transaction Mode) - Para Prisma
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct Connection - Para migraciones
DIRECT_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-us-east-1.pooler.supabase.com:5432/postgres"

# ===========================================
# SECURITY
# ===========================================
# JWT Secret (min 32 characters)
JWT_SECRET="your-super-secret-jwt-key-min-32-chars!"

# QR Encryption Key (exactly 32 characters)
QR_ENCRYPTION_KEY="your-32-character-secret-key!!"

# ===========================================
# URLS (Development Only)
# ===========================================
# En producción (Vercel), no se necesitan estas variables
# El frontend y API están en el mismo dominio

# API URL para desarrollo local
VITE_API_URL="http://localhost:3001"

# Frontend URL (for CORS in development)
FRONTEND_URL="http://localhost:5173"

# ===========================================
# ENVIRONMENT
# ===========================================
NODE_ENV="development"
