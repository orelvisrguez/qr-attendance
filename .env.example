# ============================================
# QR ATTENDANCE SYSTEM - Environment Variables
# ============================================

# ===========================================
# SUPABASE (REQUIRED)
# ===========================================
# Obtén estas keys desde: Supabase Dashboard > Settings > API

# Frontend Variables (prefijo VITE_ para acceso desde el navegador)
VITE_SUPABASE_URL="https://[PROJECT-REF].supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-key"

# Backend Variables (para serverless functions)
SUPABASE_URL="https://[PROJECT-REF].supabase.co"
SUPABASE_ANON_KEY="your-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# ===========================================
# SECURITY
# ===========================================
# JWT Secret (min 32 characters)
JWT_SECRET="your-super-secret-jwt-key-min-32-chars!"

# QR Encryption Key (exactly 32 characters)
QR_ENCRYPTION_KEY="your-32-character-secret-key!!"

# ===========================================
# DEVELOPMENT ONLY (NO configurar en Vercel!)
# ===========================================
# VITE_API_URL solo se usa en desarrollo local
# En produccion (Vercel), la API esta en el mismo dominio
# NO CONFIGURAR VITE_API_URL EN VERCEL - dejarlo vacio o no agregarlo

VITE_API_URL="http://localhost:3001"
FRONTEND_URL="http://localhost:5173"
NODE_ENV="development"

# ===========================================
# VARIABLES PARA VERCEL (copiar estas)
# ===========================================
# En Vercel Dashboard > Settings > Environment Variables:
#
# VITE_SUPABASE_URL       = https://[tu-proyecto].supabase.co
# VITE_SUPABASE_ANON_KEY  = [tu-anon-key]
# SUPABASE_URL            = https://[tu-proyecto].supabase.co
# SUPABASE_ANON_KEY       = [tu-anon-key]
# JWT_SECRET              = [cadena-aleatoria-64-chars]
# QR_ENCRYPTION_KEY       = [cadena-24-chars]
#
# IMPORTANTE: NO agregar VITE_API_URL en Vercel!
